### プロジェクト概要
この要件定義は、元の「Tube Timer」（YouTubeの視聴時間をトラッキングし、過度な利用を防ぐChrome拡張機能）を基に、X（旧Twitter）の利用時間を対象とした類似ツール「X Timer」（仮称）を作成するためのものです。目的は、ユーザーがXの閲覧時間をリアルタイムで可視化し、自己管理を促進すること。リリース可能な粒度として、MVP（Minimum Viable Product）レベルを想定し、基本機能の実装に焦点を当てます。拡張機能としてChromeブラウザ向けに開発し、Chrome Web Storeへの公開を前提とします。

### 機能要件
1. **利用時間のトラッキング**
   - Xのドメイン（x.com、twitter.com）へのアクセスを検知し、ページ滞在時間を計測。
   - アクティブタブ（フォーカスされているタブ）でのみ時間をカウント（バックグラウンド閲覧を除外）。
   - 1日の累積時間をリセットするタイミング：ブラウザ起動時またはユーザーが手動リセット可能（デフォルトは午前0時リセット）。
   - 精度：秒単位で計測、ミリ秒単位の誤差を許容。

2. **リアルタイム表示**
   - Xページの右上（または設定可能な位置）にオーバーレイUIを表示。
   - 表示内容：本日の累積利用時間（例: "本日のX時間: 1h 23m"）。
   - UI更新間隔：1秒ごと（リアルタイム感を重視）。
   - オーバーレイは非侵襲的に設計（透過度調整可能、クリックで非表示/再表示）。

3. **通知機能**
   - 利用時間が閾値を超えた場合に通知（例: 1時間超でブラウザ通知）。
   - 閾値はユーザー設定可能（デフォルト: 30分、1時間、2時間）。
   - 通知内容：利用時間を表示し、休憩を促すメッセージ（例: "X利用が1時間を超えました。休憩しませんか？"）。

4. **設定画面**
   - 拡張機能のポップアップメニューで設定可能。
   - 項目: 閾値設定、UI位置/スタイルカスタマイズ、リセットタイミング、データエクスポート（CSV形式で利用履歴出力）。
   - プライバシー考慮: 利用データはローカルストレージにのみ保存（サーバー送信なし）。

5. **リセット/履歴機能**
   - 手動リセットボタン。
   - 過去7日分の利用履歴を表示（グラフ形式で可視化、Chart.js使用）。

### 非機能要件
- **パフォーマンス**: 軽量設計。メモリ使用を最小限に抑え、ブラウザのパフォーマンスを低下させない（拡張機能のbackground scriptを効率化）。
- **セキュリティ/プライバシー**: ユーザーの閲覧内容を一切収集せず、時間のみトラッキング。Manifest V3準拠でコンテンツスクリプトを制限。
- **互換性**: Chromeブラウザ最新版対応（EdgeなどChromiumベースも考慮）。モバイルブラウザ非対応（デスクトップ優先）。
- **アクセシビリティ**: UIは高コントラスト、キーボード操作可能。
- **国際化**: 日本語/英語対応（i18nライブラリ使用）。
- **テスト**: ユニットテスト（Jest）、E2Eテスト（Puppeteer）。エッジケース（複数タブ、オフライン時）をカバー。
- **リリース基準**: Chrome Web Store審査通過（プライバシーポリシー必須）。バージョン1.0として基本機能のみ。

### 技術スタック
- **言語/フレームワーク**: JavaScript (ES6+) をメイン。React.js でUIコンポーネントを構築（ポップアップとオーバーレイ用）。
- **Chrome Extension API**:
  - `chrome.tabs`: タブのアクティブ状態検知。
  - `chrome.storage`: ローカルストレージで利用時間保存。
  - `chrome.notifications`: 通知機能。
  - `chrome.scripting`: コンテンツスクリプト注入でオーバーレイ表示。
  - Manifest V3: サービスワーカー使用。
- **UI/スタイル**: HTML5/CSS3。Tailwind CSS でレスポンシブデザイン。
- **データ可視化**: Chart.js で履歴グラフ。
- **ビルドツール**: Webpack でバンドル。TypeScript オプションで型安全化（推奨）。
- **テスト/デプロイ**: Jest for testing。Chrome Web Store Developer Dashboard で公開。
- **依存ライブラリ**: 
  - moment.js (時間フォーマット)。
  - i18next (国際化)。
  - 最小限に抑え、バンドルサイズを100KB以内に収める。
